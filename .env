# 各开关的具体耗时
# CLAUDE_CODE_COGNITIVE_LAYER（总开关）
# 本身几乎无开销；不开则下面全部跳过。

# CLAUDE_CODE_COGNITIVE_CONTEXT（上下文恢复）
# 何时跑：对话 compact 之后
# 机制：prefetch 并行预取，和现有 memory prefetch 类似；就绪后再注入
# 开销：本地扫描 transcript、打分、读/写 recovery-slices.json
# 阻塞：prefetch 若还没完成，会 await 一下（通常很短）
# CLAUDE_CODE_COGNITIVE_MEMORY_SURFACE（跨 session 记忆）
# 何时跑：仅 session 第一轮用户消息
# 开销：扫描 memory/、读文件、关键词或 embedding 排序
# 阻塞：会 await maybeAppendSessionMemorySurface()，第一轮发送前会等一下
# 记忆文件很多时，首轮可能多几百 ms～1s
# CLAUDE_CODE_COGNITIVE_MEMORY_WRITE（记忆写入）
# 何时跑：每轮结束（stop hook）
# 机制：void tryCognitiveMemoryWrite(...)，fire-and-forget，不阻塞下一轮
# 开销：后台写文件、tag 去重合并；用户基本感知不到
# CLAUDE_CODE_COGNITIVE_PLANNING（计划编排）
# 常态：读写 plan.json，开销很小
# 异常时：同一 plan step 连续 3 次可恢复错误 → 后台 fork-agent 重写 plan.md
# fork-agent 是 void 异步，不卡住主线程，但会额外消耗一次 LLM 调用（较贵，但少见）
# CLAUDE_CODE_COGNITIVE_EMBEDDING（语义排序）
# 机制：本地 n-gram TF-IDF + 余弦相似度，不调外部 API
# 缓存：索引按文件 mtime 缓存，最长 60 秒 TTL
# 开销：首次或 cache miss 时多做一些 CPU；之后很快
# 最容易感知到的「慢」
# API 变慢 / 变贵（最主要）
# 认知层会把 recovery slices、memory surface、plan 更新等作为 attachment 注入 prompt。预算上限约 30KB（直连 Anthropic）或 15KB（proxy）。
# → token 多了，模型推理时间和费用都会上升。

# Session 第一轮
# MEMORY_SURFACE 要等磁盘扫描和排序完成。

# Compact 之后的第一轮
# 要等 recovery prefetch 完成并注入。

# Planning 连续失败
# 会悄悄跑一个 fork-agent replan（额外 LLM 成本，不影响你继续打字）。

# 实用建议
# 日常开发、追求速度：只开 LAYER + CONTEXT，或再加 PLANNING
# 需要跨 session 记忆：加 MEMORY_SURFACE + MEMORY_WRITE（写入不阻塞，surface 只影响首轮）
# 记忆/恢复召回不准：最后加 EMBEDDING（纯本地 CPU，有缓存）
# 最省：全部关闭，和没装认知层一样
# 一句话：本地开销可控且多数在后台；真正让对话「更慢更贵」的，主要是每轮多注入的 context token，以及 planning 失败时的偶发额外 LLM 调用。


CLAUDE_CODE_COGNITIVE_LAYER=0
CLAUDE_CODE_COGNITIVE_CONTEXT=0      # TranscriptRecoveryIndex
CLAUDE_CODE_COGNITIVE_MEMORY_SURFACE=0  # Phase 1.5 read path
CLAUDE_CODE_COGNITIVE_PLANNING=0    # Phase 2
CLAUDE_CODE_COGNITIVE_MEMORY_WRITE=0 # Phase 3 write path
CLAUDE_CODE_COGNITIVE_EMBEDDING=0   # Phase 3.1